Мысли в слух, описывающие что делать дальше:

- Для регистрации callback использовать libffi (ffi_closure). Разобраться с 
удалением уже ненужных closures. Самое простое: завести специальный обхек (как в ctypes)
и оборачивать им питоновский объект:
def timer_proc(): pass
timer_proc = CTIMERPROC(timer_proc)
главное, чтобы timer_proc не удалился до того как он еще может потребоваться.

#- висячие ссылки на callback функции и внутренние данные чистить когда модуль выгружается

- все оставшиеся функциий inkview.h
- icanvas
- ibitmap 
- ...

- сообщения об ошибках

- автоматически делать %include только констант. Прототипы функций подключать явно.
- по возможности убрать дублирование кода (использовать макросы в swig)

- setup.py для сборки и реорганизовать структуру каталогов, чтобы был нормальный
  питоновский модуль
