Мысли в слух, описывающие что делать дальше:

№- таймер не реализовать нормально (нельзя создавать несколько таймеров одновременно)
№  пока нет дополнительного параметра в обработчике (возможно, ffi может динамически 
№  создавать callback функции.. хотя, это, имхо, на грани фантастики - проверить)
То что выше, похоже, касается и обычных Events. Самое простое - нагенерировать 
массив функций каждого типа (остается проблема с очисткой уже неиспользуемых указателей). 
В libffi (тот что используется в ctypes), вроде бы используется другой подход подход.
Надо разобраться с куском на ассемблере.

- висячие ссылки на callback функции и внутренние данные чистить когда модуль выгружается
- Баг: В начале надо добавлять ссылку на функцию, а только потом ее удалять

- все оставшиеся функциий inkview.h
- icanvas
- ibitmap 
- ...

- сообщения об ошибках

- автоматически делать %include только констант. Прототипы функций подключать явно.
- по возможности убрать дублирование кода (использовать возможности swig)

- setup.py для сборки и реорганизовать структуру каталогов, чтобы был нормальный
  питоновский модуль
