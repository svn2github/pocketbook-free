// Для обложки в prc-файлах //
HtmlBookReader.cpp
MobipocketHtmlBookReader.cpp
MobipocketStream.cpp
// Для обложки в prc-файлах //

// Содержание в PRC-файлах нормально работает. Показывает тоже самое что и другие ридеры FBReader 0.10.7 и MobiPocket 6.2 Build 608.

// Исправил копирование для иврита, теперь оно не копирует, а просто берет указатель и конвертит. 
// Раньше чего-то не работало, теперь получилось.
// Потестируй, посмотри чтоб правильно. Вроде нормально выводит.