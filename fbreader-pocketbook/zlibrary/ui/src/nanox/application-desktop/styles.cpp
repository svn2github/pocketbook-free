#include <inkview.h>
#include <inkinternal.h>

#include "main.h"

/*
void read_styles() {

	std::string stylesname = "/mnt/ext1/system/config/styles.xml";
	if (stat(stylesname.c_str(), &st) == -1) {
		stylesname = ZLibrary::DefaultFilesPathPrefix() + "styles.xml";
	}
	fprintf(stderr, "using %s\n", stylesname.c_str());
*/